name: IITM test action

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 23f3002777@ds.study.iitm.ac.in
        run: echo "Hello, world!"
